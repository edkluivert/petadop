import 'package:flutter/material.dart';

class DetailTabletLayout extends StatelessWidget {
  const DetailTabletLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}