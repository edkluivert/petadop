import 'package:flutter/material.dart';

class DetailDesktopLayout extends StatelessWidget {
  const DetailDesktopLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}